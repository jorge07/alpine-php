version: '2'

services:
  nginx:
      build:
        context: .
        dockerfile: nginx/Dockerfile
      depends_on:
        - fpm

  fpm:
    image: jorge07/alpine-php:7
    expose:
      - 9000
    volumes:
      - "$PWD/test:/app"
